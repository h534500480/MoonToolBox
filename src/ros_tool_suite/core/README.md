# Core 层说明

这里用于放和界面无关的业务逻辑。

建议后续逐步把下面这些能力从工具页里拆出来：

- `mapping/`
  点云到地图、栅格化、障碍物膨胀、区域分类
- `perception/`
  costmap 解析、回放数据模型、导出
- `network/`
  网段扫描、设备信息解析、结果模型

目标是：

- 桌面 UI 只负责交互
- `core` 负责真正逻辑
- 后续 Web 或 C++ 接入时可以直接复用这一层
